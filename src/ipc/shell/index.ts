import {
  openExternalLink,
  pickAudioFile,
  pickLocalFiles,
  saveRecordedAudio,
} from "./handlers";

export const shell = {
  openExternalLink,
  pickAudioFile,
  pickLocalFiles,
  saveRecordedAudio,
};
